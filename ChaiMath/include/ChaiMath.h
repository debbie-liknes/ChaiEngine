#pragma once
#include <VecBase.h>
#include <ChaiMatrix.h>
#include <ChaiQuat.h>
#include <ChaiMathOps.h>

namespace chai
{
	// Operator* instantiations
	//template CHAIMATH_EXPORT Mat<float, 2> operator*(const Mat<float, 2>&, const Mat<float, 2>&);
	//template CHAIMATH_EXPORT Mat<float, 3> operator*(const Mat<float, 3>&, const Mat<float, 3>&);
	//template CHAIMATH_EXPORT Mat<float, 4> operator*(const Mat<float, 4>&, const Mat<float, 4>&);

	//template CHAIMATH_EXPORT Vec<float, 2> operator-(const Vec<float, 2>&, const Vec<float, 2>&);
	//template CHAIMATH_EXPORT Vec<float, 2> operator-(const Vec<float, 2>&);
}