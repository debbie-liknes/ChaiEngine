[project]
name = "chaiengine"
version = "0.1.0"
readme = "README.md"
authors = []
requires-python = ">=3.13"
dependencies = [
    "breathe>=4.36.0",
    "sphinx>=9.1.0",
    "sphinx-rtd-theme>=3.1.0",
]

[tool.uv]
package = false

[build-system]
requires = ["uv_build>=0.12.1,<0.13.0"]
build-backend = "uv_build"
